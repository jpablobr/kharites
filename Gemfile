source :rubygems

# Project requirements
gem 'rake'
gem 'rack-flash'
gem 'thin' # or mongrel

# Component requirements
gem 'maruku'
gem 'haml'
gem 'sinatra'
gem 'haml', '3.0.2'
gem 'hassle'

# Beta env for mongoid because of undefined method `assert_valid_keys' error
gem 'mongo', '0.20.1'
gem 'bson_ext', '0.20.1', :require => "mongo"
gem 'mongoid', '2.0.0.beta4'
gem 'will_paginate', '3.0.pre'
gem 'activemodel', '3.0.0.beta3'
# Beta 

gem 'Kharites-tools'
# When my fixes are merged in switch back to the official version
gem 'jpablobr-sinatra-authorization', :require => "sinatra/authorization"

# Test requirements
gem 'rr', :group => "test"
gem 'rspec', :require => "spec", :group => "test"
gem 'rack-test', :require => 'rack/test', :group => 'test'
